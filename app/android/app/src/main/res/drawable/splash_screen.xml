<?xml version="1.0" encoding="utf-8"?>
<!--
  Launch screen: brand green with the mark centred.

  A layer-list over vectors rather than the eleven density-specific splash.png
  files the Capacitor template ships. Those are still on disk but unreferenced;
  this scales exactly on every screen and is a few hundred bytes.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item
        android:gravity="center"
        android:width="150dp"
        android:height="150dp"
        android:drawable="@drawable/ic_launcher_foreground" />
</layer-list>
